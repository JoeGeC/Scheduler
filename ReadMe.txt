To run this program, run the SchedulerParser.exe located in SchedulerParser/bin/Debug.
If you're on mac, use "mono SchedulerParser.exe" on the command line.

Unit tests are located in the Tests directory.

ExampleConfig.txt is the example config given for the test. It can be used as an acceptance test.

Cheers!